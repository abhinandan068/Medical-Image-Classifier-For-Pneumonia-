# Medical-Image-Classifier-For-Pneumonia-
Grad-CAM visualizes how models focus on regions for predictions. In the Chest X-ray dataset, images are split into NORMAL and PNEUMONIA under train, val, and test. Using os.listdir() and random.choice(), images are loaded dynamically, ensuring flexible preprocessing and accurate heatmap visualization.
